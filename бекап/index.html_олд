<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Smart Assistant (Single File)</title>
    <style>
        /* --- СОДЕРЖИМОЕ styles.css --- */
        /* Общие стили */
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f7f6;
            color: #333;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .app-container {
            width: 95%;
            max-width: 1400px;
            background-color: #ffffff;
            border-radius: 8px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            display: flex;
            flex-direction: column;
            height: 90vh; /* Ограничиваем высоту */
        }

        /* Шапка */
        .app-header {
            background-color: #4a90e2;
            color: white;
            padding: 15px 25px;
            border-bottom: 1px solid #ddd;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .app-header h1 {
            margin: 0;
            font-size: 1.5em;
        }

        .notifications {
            min-height: 20px;
            font-size: 0.9em;
        }

        .notification {
            padding: 5px 10px;
            border-radius: 4px;
            margin-left: 10px;
            color: white;
            display: inline-block; /* Чтобы уведомления были в строке, если их несколько */
        }
        .notification.success { background-color: #4CAF50; }
        .notification.error { background-color: #f44336; }
        .notification.warning { background-color: #ff9800; color: #333; }

        /* Основной контент */
        .app-main {
            display: flex;
            flex-grow: 1;
            overflow: hidden; /* Предотвращаем выход контента */
        }

        /* Боковая панель (Файловый менеджер) */
        .sidebar {
            width: 250px;
            background-color: #e9ecef;
            padding: 15px;
            border-right: 1px solid #d1dbe5;
            display: flex;
            flex-direction: column;
            overflow-y: auto; /* Добавляем скроллбар */
        }

        .sidebar h2 {
            margin-top: 0;
            font-size: 1.2em;
            color: #4a5568;
            border-bottom: 1px solid #ccc;
            padding-bottom: 10px;
        }

        .directory-controls {
            margin-bottom: 10px;
            display: flex;
            align-items: center;
            gap: 5px;
            flex-wrap: wrap;
        }
        #current-path {
            font-weight: bold;
            word-break: break-all; /* Перенос длинных путей */
        }

        #file-list {
            list-style: none;
            padding: 0;
            margin: 0;
            flex-grow: 1;
        }

        #file-list li {
            padding: 8px 12px;
            cursor: pointer;
            border-radius: 4px;
            margin-bottom: 4px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            transition: background-color 0.2s ease;
            white-space: nowrap; /* Предотвращаем перенос имени файла */
            overflow: hidden;
            text-overflow: ellipsis; /* Добавляем троеточие, если не помещается */
        }

        #file-list li:hover {
            background-color: #d1dbe5;
        }

        #file-list li.loading {
            font-style: italic;
            color: #666;
        }
        #file-list li.error { color: #f44336; }

        #file-list li.directory::before {
            content: '📁'; /* Иконка папки */
            margin-right: 8px;
            font-size: 1.1em;
            flex-shrink: 0;
        }
        #file-list li.file::before {
            content: '📄'; /* Иконка файла */
            margin-right: 8px;
            flex-shrink: 0;
        }

        /* Область контента */
        .content-area {
            flex-grow: 1;
            display: flex;
            flex-direction: column; /* Располагаем элементы вертикально */
            padding: 20px;
            background-color: #fdfdfd;
            overflow-y: auto; /* Скролл для всей области */
        }

        /* Просмотрщик/редактор файлов */
        .file-viewer {
            flex-grow: 1; /* Занимает доступное пространство */
            display: flex;
            flex-direction: column;
            margin-bottom: 20px; /* Отступ от чата */
            min-height: 250px; /* Минимальная высота редактора */
        }

        .file-viewer h2 {
            margin-top: 0;
            font-size: 1.2em;
            color: #4a5568;
        }

        #file-info {
            margin-bottom: 10px;
            color: #555;
            background: #eee;
            padding: 5px 8px;
            border-radius: 3px;
            min-height: 20px;
            font-size: 0.9em;
        }

        #file-content-editor {
            width: 100%;
            flex-grow: 1; /* Позволяет textarea растягиваться */
            border: 1px solid #ccc;
            border-radius: 4px;
            padding: 10px;
            font-family: 'Courier New', Courier, monospace;
            font-size: 0.95em;
            box-sizing: border-box; /* Учитываем padding и border в размерах */
            min-height: 150px; /* Начальная высота */
            resize: vertical; /* Разрешить изменение размера только по вертикали */
        }

        .file-actions {
            margin-top: 15px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        #file-status {
            font-size: 0.9em;
            color: #666;
            margin-left: auto; /* Прижимаем статус к правому краю */
        }

        .config-display {
            margin-top: 15px;
            border-top: 1px solid #eee;
            padding-top: 10px;
        }
        .config-display h3 { margin-top: 0; font-size: 1em; }
        .config-display pre {
            background-color: #f0f0f0;
            padding: 10px;
            border-radius: 4px;
            white-space: pre-wrap;
            word-wrap: break-word;
            max-height: 150px; /* Ограничиваем высоту блока с конфигом */
            overflow-y: auto;
            font-size: 0.9em;
        }

        /* Интерфейс чата */
        .chat-interface {
            border-top: 1px solid #e0e0e0;
            padding-top: 20px;
            display: flex;
            flex-direction: column;
            min-height: 200px; /* Мин. высота чата */
            flex-shrink: 0; /* Не сжиматься */
        }

        .chat-interface h2 {
            margin-top: 0;
            font-size: 1.2em;
            color: #4a5568;
        }

        #chat-history {
            flex-grow: 1;
            overflow-y: auto;
            border: 1px solid #e0e0e0;
            border-radius: 4px;
            padding: 10px;
            margin-bottom: 10px;
            min-height: 100px;
            background-color: #f9f9f9;
        }

        .chat-message {
            margin-bottom: 8px;
            padding: 6px 12px;
            border-radius: 15px;
            max-width: 85%;
            word-wrap: break-word;
            line-height: 1.4;
        }

        .chat-message.user {
            background-color: #dcf8c6;
            margin-left: auto;
            border-bottom-right-radius: 5px;
        }

        .chat-message.agent {
            background-color: #e1f5fe;
            margin-right: auto;
            border-bottom-left-radius: 5px;
        }
         .chat-message.error {
            background-color: #ffebee;
            color: #c62828;
             margin-right: auto;
            border-bottom-left-radius: 5px;
            font-style: italic;
         }

        .chat-message .sender {
            font-weight: bold;
            font-size: 0.8em;
            margin-bottom: 3px;
            color: #555;
        }
        .chat-message .timestamp {
            font-size: 0.7em;
            color: #999;
            margin-top: 4px;
            text-align: right;
            display: block; /* Чтобы был на своей строке */
        }


        .chat-input-area {
             display: flex;
             flex-direction: column; /* Поле для ключа над полем ввода */
             gap: 5px;
        }

        .api-key-area {
            display: flex;
            align-items: center;
            gap: 5px;
            font-size: 0.85em;
        }
         .api-key-area label {
            flex-shrink: 0;
         }
         #api-key-input {
             flex-grow: 1;
             padding: 4px 8px;
             border: 1px solid #ccc;
             border-radius: 4px;
             font-size: 0.9em;
         }

        .chat-input {
            display: flex;
            gap: 10px;
        }

        #chat-message-input {
            flex-grow: 1;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 20px;
        }

        /* Подвал */
        .app-footer {
            background-color: #f8f9fa;
            color: #6c757d;
            padding: 10px 25px;
            text-align: center;
            font-size: 0.9em;
            border-top: 1px solid #e0e0e0;
            flex-shrink: 0; /* Не сжиматься */
        }

        /* Кнопки */
        button {
            padding: 8px 15px;
            border: none;
            border-radius: 5px;
            background-color: #4a90e2;
            color: white;
            cursor: pointer;
            transition: background-color 0.2s ease, box-shadow 0.2s ease;
            font-size: 0.9em;
        }

        button:hover:not(:disabled) {
            background-color: #357abd;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }
        button:active:not(:disabled) {
            background-color: #2a6ca4;
        }

        button:disabled {
            background-color: #cccccc;
            cursor: not-allowed;
            box-shadow: none;
        }

        #btn-root, #btn-create-file {
            padding: 5px 10px;
        }
        #btn-create-file {
            background-color: #5cb85c;
        }
        #btn-create-file:hover:not(:disabled) {
            background-color: #4cae4c;
        }
        #btn-delete-file {
            background-color: #d9534f;
        }
        #btn-delete-file:hover:not(:disabled) {
            background-color: #c9302c;
        }
        #btn-send-message {
            flex-shrink: 0; /* Не сжимать кнопку отправки */
        }
    </style>
</head>
<body>
    <div class="app-container">
        <header class="app-header">
            <h1>Smart Assistant (SF)</h1>
            <div id="notifications" class="notifications"></div>
        </header>

        <main class="app-main">
            <aside class="sidebar file-explorer">
                <h2>Файловый менеджер</h2>
                <div class="directory-controls">
                    <button id="btn-root" title="Перейти в корень">/ Корневая</button>
                    <button id="btn-create-file" title="Создать файл в текущей папке">+</button>
                 </div>
                 <div id="current-path" style="word-break: break-all; font-weight: bold; margin: 5px 0; font-size: 0.9em;">/</div>
                <ul id="file-list">
                    <!-- Список файлов будет здесь -->
                    <li class="loading">Загрузка...</li>
                </ul>
            </aside>

            <section class="content-area">
                <div id="file-viewer" class="file-viewer">
                    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 10px;">
                       <h2 style="margin: 0;">Просмотр / Редактирование</h2>
                       <span id="file-status"></span>
                   </div>
                    <div id="file-info">Выберите файл для просмотра</div>
                    <textarea id="file-content-editor" placeholder="Содержимое файла..." disabled></textarea>
                    <div class="file-actions">
                        <button id="btn-save-file" disabled>Сохранить</button>
                        <button id="btn-delete-file" disabled>Удалить</button>
                    </div>
                     <div id="agent-config-display" class="config-display" style="display: none;">
                        <h3>Конфигурация Активного Агента (из файла):</h3>
                        <pre id="agent-config-data"></pre>
                    </div>
                </div>

                <div id="chat-interface" class="chat-interface">
                    <h2>Чат с Агентом (VseGPT API)</h2>
                    <div id="chat-history">
                        <div class="chat-message agent">
                            <div class="sender">Агент</div>
                            <div>Привет! Выберите файл чата в /chats/ или создайте новый. Затем введите API ключ и ваше сообщение.</div>
                            <div class="timestamp"></div>
                        </div>
                     </div>
                     <div class="chat-input-area">
                         <div class="api-key-area">
                             <label for="api-key-input">VseGPT API Ключ:</label>
                            <input type="password" id="api-key-input" placeholder="sk-...">
                         </div>
                         <div class="chat-input">
                            <input type="text" id="chat-message-input" placeholder="Введите сообщение..." disabled>
                            <button id="btn-send-message" disabled>Отправить</button>
                         </div>
                    </div>
                </div>
            </section>
        </main>

        <footer class="app-footer">
            <p>Smart Assistant (Single File) © 2024</p>
        </footer>
    </div>

    <!-- ================================= SCRIPT ================================= -->
    <script>
        // Оборачиваем весь код в IIFE для изоляции области видимости (хотя в single file это менее критично)
        (async () => {
            console.log('Smart Assistant (SF): Инициализация...');

            // --- Глобальные DOM Элементы (доступны во всем скрипте) ---
            const fileListElement = document.getElementById('file-list');
            const fileContentEditor = document.getElementById('file-content-editor');
            const fileInfoElement = document.getElementById('file-info');
            const fileStatusElement = document.getElementById('file-status');
            const saveButton = document.getElementById('btn-save-file');
            const deleteButton = document.getElementById('btn-delete-file');
            const notificationsElement = document.getElementById('notifications');
            const currentPathElement = document.getElementById('current-path');
            const rootButton = document.getElementById('btn-root');
            const createFileButton = document.getElementById('btn-create-file');
            const agentConfigDisplay = document.getElementById('agent-config-display');
            const agentConfigData = document.getElementById('agent-config-data');
            const chatHistoryElement = document.getElementById('chat-history');
            const chatMessageInput = document.getElementById('chat-message-input');
            const sendMessageButton = document.getElementById('btn-send-message');
            const apiKeyInput = document.getElementById('api-key-input');

            // --- Состояние приложения ---
            let currentDirectory = '/';
            let selectedFilePath = null;
            let isEditorDirty = false;
            let currentChatFile = null; // { filePath: string, messages: Array<object> }
            let activeAgentConfig = null; // Конфигурация активного агента (из файла)

            // ----------------------------------------------------------------------
            // --- Utils ---
            // ----------------------------------------------------------------------
            const Utils = {
                generateId: () => 'id_' + Date.now() + '_' + Math.random().toString(36).substring(2, 9),
                formatTimestamp: (timestamp) => {
                    try {
                        const date = new Date(timestamp);
                        if (isNaN(date.getTime())) return ''; // Не показывать время для сообщений без метки
                        return date.toLocaleString('ru-RU', { hour: '2-digit', minute: '2-digit', second: '2-digit' });
                    } catch (e) { return ''; }
                },
                getFileName: (filePath) => {
                    if (!filePath || typeof filePath !== 'string') return '';
                    const parts = filePath.split('/');
                    return parts[parts.length - 1];
                },
                getDirectory: (filePath) => {
                    if (!filePath || typeof filePath !== 'string') return '/';
                    const lastSlashIndex = filePath.lastIndexOf('/');
                    return lastSlashIndex <= 0 ? '/' : filePath.substring(0, lastSlashIndex + 1);
                },
                createBackupPath: (fileData) => {
                    const timestampSuffix = new Date(fileData.timestamp).toISOString().replace(/[:.]/g, '-');
                    const nameParts = fileData.name.split('.');
                    const extension = nameParts.length > 1 ? '.' + nameParts.pop() : '';
                    const baseName = nameParts.join('.');
                    const backupName = `${baseName}_${timestampSuffix}${extension}`;
                    return `/backup/${backupName}`;
                }
            };

            // ----------------------------------------------------------------------
            // --- DB Manager (IndexedDB) ---
            // ----------------------------------------------------------------------
            const DBManager = (() => {
                const DB_NAME = 'smartAssistantDB_SF'; // Новое имя для single file версии
                const DB_VERSION = 1; // Можно увеличить, если структура меняется
                const STORE_NAME = 'files';
                let db = null;

                /** Добавляет стартовые файлы, если еще не существуют */
                function addInitialData(store) {
                     console.log("Проверка и добавление начальных данных...");
                     const now = Date.now();
                     const initialFiles = [
                         {
                             filePath: '/chats/welcome.json', directory: '/chats/', name: 'welcome.json', timestamp: now,
                             content: JSON.stringify({
                                 id: Utils.generateId(),
                                 messages: [{ sender: 'agent', text: 'Добро пожаловать! Это пример чата.', timestamp: now }]
                             }, null, 2)
                         },
                         {
                             filePath: '/agents/example-agent.json', directory: '/agents/', name: 'example-agent.json', timestamp: now,
                             content: JSON.stringify({
                                 id: 'agent_example_1', name: 'Example Agent',
                                 configurations: { model: 'anthropic/claude-3-haiku', temperature: 0.7, greeting: 'Hello from Example Agent!' }
                             }, null, 2)
                         },
                         {
                            filePath: '/agents/openai-gpt4o.json', directory: '/agents/', name: 'openai-gpt4o.json', timestamp: now,
                            content: JSON.stringify({
                                id: 'agent_openai_01', name: 'OpenAI GPT-4o',
                                configurations: { model: 'openai/gpt-4o', temperature: 0.8, max_tokens: 3000 }
                            }, null, 2)
                        },
                         {
                             filePath: '/secrets/sample-credentials.json', directory: '/secrets/', name: 'sample-credentials.json', timestamp: now,
                             content: JSON.stringify({ // Без шифрования!
                                 id: 'secret_1', service: 'MyService', username: 'user', notes: 'Store API keys or other secrets here.'
                             }, null, 2)
                         }
                     ];

                    // Используем Promise.all для асинхронного добавления
                    const putPromises = initialFiles.map(fileData => {
                         return new Promise((resolve, reject) => {
                             // Проверяем, существует ли файл, прежде чем добавлять
                             const getRequest = store.get(fileData.filePath);
                             getRequest.onsuccess = (event) => {
                                 if (!event.target.result) { // Если файла нет
                                     const putRequest = store.put(fileData);
                                     putRequest.onsuccess = () => {
                                         console.log(`Добавлен начальный файл: ${fileData.filePath}`);
                                         resolve();
                                     };
                                     putRequest.onerror = (e) => {
                                         console.error(`Ошибка добавления ${fileData.filePath}:`, e.target.error);
                                         reject(e.target.error); // Прерываем Promise.all при ошибке
                                     };
                                 } else {
                                     // Файл уже существует, ничего не делаем
                                    console.log(`Начальный файл уже существует: ${fileData.filePath}`);
                                     resolve();
                                 }
                             };
                             getRequest.onerror = (e) => { // Ошибка при проверке
                                console.error(`Ошибка проверки ${fileData.filePath}:`, e.target.error);
                                reject(e.target.error);
                            };
                         });
                     });

                     return Promise.all(putPromises); // Возвращаем промис, который разрешится, когда все добавления/проверки завершатся
                 }


                function openDB() {
                    return new Promise((resolve, reject) => {
                        if (db) { resolve(db); return; }
                        const request = indexedDB.open(DB_NAME, DB_VERSION);

                        request.onerror = (event) => {
                            console.error(`[DB] Ошибка открытия:`, event.target.error);
                            reject(`Не удалось открыть базу данных: ${event.target.error}`);
                        };

                        request.onsuccess = (event) => {
                            db = event.target.result;
                            console.log(`[DB] База ${DB_NAME} v${DB_VERSION} открыта.`);
                            db.onerror = (e) => console.error(`[DB] Глобальная ошибка:`, e.target.error);
                            resolve(db);
                        };

                        request.onupgradeneeded = async (event) => {
                            console.log(`[DB] Обновление до v${DB_VERSION}...`);
                            const tempDb = event.target.result;
                            const transaction = event.target.transaction; // Используем транзакцию обновления
                            let store;

                            if (!tempDb.objectStoreNames.contains(STORE_NAME)) {
                                console.log(`[DB] Создание хранилища: ${STORE_NAME}`);
                                store = tempDb.createObjectStore(STORE_NAME, { keyPath: 'filePath' });
                                store.createIndex('directoryIndex', 'directory', { unique: false });
                                console.log(`[DB] Хранилище и индекс созданы.`);
                           } else {
                                console.log(`[DB] Хранилище ${STORE_NAME} уже существует.`);
                               store = transaction.objectStore(STORE_NAME); // Получаем существующее
                           }

                            // Добавляем начальные данные в рамках той же транзакции обновления
                            try {
                                // Важно: addInitialData теперь асинхронная (использует проверку get)
                                // В onupgradeneeded промисы могут быть сложными, лучше использовать `await` внутри если возможно,
                                // но т.к. addInitialData теперь сама проверяет get, можно просто вызвать store.put
                                // Переделываем addInitialData, чтобы она была синхронной для onupgradeneeded

                                console.log("Добавление начальных данных (синхронно внутри onupgradeneeded)...");
                                const now = Date.now();
                                const initialFiles = [
                                    // ... (копии данных файлов как выше)
                                    { filePath: '/chats/welcome.json', directory: '/chats/', name: 'welcome.json', timestamp: now, content: JSON.stringify({ id: Utils.generateId(), messages: [{ sender: 'agent', text: 'Добро пожаловать! Это пример чата.', timestamp: now }] }, null, 2) },
                                    { filePath: '/agents/example-agent.json', directory: '/agents/', name: 'example-agent.json', timestamp: now, content: JSON.stringify({ id: 'agent_example_1', name: 'Example Agent', configurations: { model: 'anthropic/claude-3-haiku', temperature: 0.7, greeting: 'Hello!' } }, null, 2) },
                                    { filePath: '/agents/openai-gpt4o.json', directory: '/agents/', name: 'openai-gpt4o.json', timestamp: now, content: JSON.stringify({ id: 'agent_openai_01', name: 'OpenAI GPT-4o', configurations: { model: 'openai/gpt-4o', temperature: 0.8 } }, null, 2) },
                                    { filePath: '/secrets/sample-credentials.json', directory: '/secrets/', name: 'sample-credentials.json', timestamp: now, content: JSON.stringify({ id: 'secret_1', service: 'MyService', username: 'user', notes: 'API keys etc.' }, null, 2) }
                                ];

                                initialFiles.forEach(fileData => {
                                    // В onupgradeneeded можно просто делать put/add без предварительной проверки get,
                                    // но нужно обернуть в try/catch на случай, если первичный ключ уже существует (хотя не должен при создании)
                                    try {
                                        store.add(fileData); // Используем add, чтобы вызвать ошибку, если ключ есть
                                        console.log(`[DB Init] Запланировано добавление: ${fileData.filePath}`);
                                    } catch (e) {
                                         // Ошибка ConstraintError ожидаема, если файл уже существует (при обновлении версии, но не изменении структуры)
                                         if (e.name === 'ConstraintError') {
                                             console.warn(`[DB Init] Файл ${fileData.filePath} уже существует, пропуск.`);
                                         } else {
                                            console.error(`[DB Init] Ошибка добавления ${fileData.filePath}:`, e);
                                        }
                                    }
                                });
                                console.log("[DB Init] Добавление начальных данных запланировано.");

                            } catch (error) {
                                console.error("[DB] Ошибка при добавлении начальных данных:", error);
                                event.target.transaction.abort(); // Откатываем транзакцию при ошибке
                                reject(error);
                                return;
                            }

                            // В onupgradeneeded не нужно ждать resolve/reject промисов, транзакция завершится сама
                        };
                    });
                }

                function getDB() { return db; }

                return { openDB, getDB, STORE_NAME };
            })();

            // ----------------------------------------------------------------------
            // --- FileSystem API (Взаимодействие с DB) ---
            // ----------------------------------------------------------------------
            const FileSystemAPI = (() => {
                async function openDB() {
                    try { return await DBManager.openDB(); }
                    catch (error) { console.error("API: Ошибка открытия БД:", error); throw error; }
                }

                async function listFiles(directory) {
                    if (!directory.endsWith('/')) directory += '/';
                    if (directory === '//') directory = '/';
                    console.log(`API: listFiles для ${directory}`);
                    const db = await openDB();
                    return new Promise((resolve, reject) => {
                        const transaction = db.transaction(DBManager.STORE_NAME, 'readonly');
                        const store = transaction.objectStore(DBManager.STORE_NAME);
                        const getAllRequest = store.getAll();
                        const items = new Map();

                        getAllRequest.onsuccess = (event) => {
                            const allFiles = event.target.result;
                             allFiles.forEach(file => {
                                 const fileDir = file.directory; // e.g., /chats/
                                 const filePath = file.filePath; // e.g., /chats/mychat.json

                                 // 1. Файлы непосредственно в запрашиваемой директории
                                 if (fileDir === directory) {
                                     items.set(filePath, { name: file.name, type: 'file', path: filePath });
                                 }
                                 // 2. Поддиректории внутри запрашиваемой директории
                                 else if (fileDir.startsWith(directory) && fileDir !== directory) {
                                     // Извлекаем имя поддиректории первого уровня
                                     const relativePath = fileDir.substring(directory.length); // e.g., subdir1/subdir2/
                                     const dirName = relativePath.split('/')[0]; // e.g., subdir1
                                     if (dirName) {
                                         const dirPath = `${directory}${dirName}/`;
                                         if (!items.has(dirPath)) {
                                             items.set(dirPath, { name: dirName, type: 'directory', path: dirPath });
                                         }
                                     }
                                 }
                             });

                             const sortedItems = Array.from(items.values()).sort((a, b) => {
                                 if (a.type !== b.type) return a.type === 'directory' ? -1 : 1;
                                 return a.name.localeCompare(b.name);
                             });
                             resolve(sortedItems);
                        };
                        getAllRequest.onerror = (event) => {
                            console.error(`API: Ошибка listFiles ${directory}:`, event.target.error);
                            reject(`Ошибка чтения списка файлов: ${event.target.error}`);
                        };
                    });
                }

                async function getFile(filePath) {
                    console.log(`API: getFile ${filePath}`);
                    const db = await openDB();
                    return new Promise((resolve, reject) => {
                        const transaction = db.transaction(DBManager.STORE_NAME, 'readonly');
                        const store = transaction.objectStore(DBManager.STORE_NAME);
                        const request = store.get(filePath);
                        request.onsuccess = (event) => {
                            const fileData = event.target.result;
                            // !!! Шифрование убрано !!!
                            resolve(fileData || null);
                        };
                        request.onerror = (event) => {
                            console.error(`API: Ошибка getFile ${filePath}:`, event.target.error);
                            reject(`Ошибка получения файла: ${event.target.error}`);
                        };
                    });
                }

                async function saveFile(filePath, content) {
                    console.log(`API: saveFile ${filePath}`);
                    const db = await openDB();
                    const directory = Utils.getDirectory(filePath);
                    const name = Utils.getFileName(filePath);
                    const timestamp = Date.now();
                     // !!! Шифрование убрано !!!
                    const processedContent = typeof content === 'string' ? content : JSON.stringify(content, null, 2);
                    const fileData = { filePath, directory, name, content: processedContent, timestamp };

                    return new Promise(async (resolve, reject) => {
                        const existingFile = await getFile(filePath).catch(() => null);
                        const performSaveTransaction = (backupDone = false) => {
                            const transaction = db.transaction(DBManager.STORE_NAME, 'readwrite');
                            const store = transaction.objectStore(DBManager.STORE_NAME);
                            const saveRequest = store.put(fileData);
                            transaction.oncomplete = () => {
                                console.log(`API: Файл ${filePath} сохранен.`);
                                resolve(fileData); // Возвращаем с необработанным контентом
                            };
                            transaction.onerror = (event) => {
                                console.error(`API: Ошибка транзакции сохранения ${filePath}:`, event.target.error);
                                reject(`Ошибка транзакции сохранения: ${event.target.error}`);
                            };
                        };

                        if (existingFile && existingFile.content !== fileData.content && directory!=='/backup/') { // Не бэкапим бэкапы и если контент не изменился
                            console.log(`API: Создание бэкапа для ${filePath}...`);
                            const backupPath = Utils.createBackupPath(existingFile);
                             // Копируем данные существующего файла, включая его контент (уже нешифрованный)
                            const backupData = { ...existingFile, filePath: backupPath, directory: '/backup/', name: Utils.getFileName(backupPath), timestamp: Date.now() };
                             // Убедимся что контент строка для бэкапа
                             backupData.content = typeof backupData.content === 'string' ? backupData.content : JSON.stringify(backupData.content, null, 2);

                            const backupTransaction = db.transaction(DBManager.STORE_NAME, 'readwrite');
                            backupTransaction.objectStore(DBManager.STORE_NAME).put(backupData);
                            backupTransaction.oncomplete = () => {
                                console.log(`API: Бэкап создан: ${backupPath}`);
                                performSaveTransaction(true);
                            };
                            backupTransaction.onerror = (event) => {
                                console.error(`API: Ошибка бэкапа ${filePath}:`, event.target.error);
                                performSaveTransaction(false); // Все равно сохраняем основной файл
                            };
                        } else {
                            performSaveTransaction(false);
                        }
                    });
                }

                async function deleteFile(filePath) {
                    console.log(`API: deleteFile ${filePath}`);
                    const db = await openDB();
                    return new Promise(async (resolve, reject) => {
                        const fileToBackup = await getFile(filePath).catch(() => null);
                        const performDeleteTransaction = (backupDone = false) => {
                            const transaction = db.transaction(DBManager.STORE_NAME, 'readwrite');
                            transaction.objectStore(DBManager.STORE_NAME).delete(filePath);
                            transaction.oncomplete = () => { console.log(`API: Файл ${filePath} удален.`); resolve(); };
                            transaction.onerror = (event) => { console.error(`API: Ошибка удаления ${filePath}:`, event.target.error); reject(`Ошибка удаления: ${event.target.error}`); };
                        };

                         if (fileToBackup && Utils.getDirectory(filePath) !=='/backup/') { // Не бэкапим бэкапы при удалении
                             console.log(`API: Создание бэкапа перед удалением ${filePath}...`);
                             const backupPath = Utils.createBackupPath(fileToBackup);
                             const backupData = { ...fileToBackup, filePath: backupPath, directory: '/backup/', name: Utils.getFileName(backupPath), timestamp: Date.now() };
                             // Убедимся что контент строка для бэкапа
                             backupData.content = typeof backupData.content === 'string' ? backupData.content : JSON.stringify(backupData.content, null, 2);

                             const backupTransaction = db.transaction(DBManager.STORE_NAME, 'readwrite');
                             backupTransaction.objectStore(DBManager.STORE_NAME).put(backupData);
                             backupTransaction.oncomplete = () => { console.log(`API: Бэкап перед удалением создан: ${backupPath}`); performDeleteTransaction(true); };
                             backupTransaction.onerror = (event) => { console.error(`API: Ошибка бэкапа перед удалением ${filePath}:`, event.target.error); performDeleteTransaction(false); };
                         } else {
                              if(Utils.getDirectory(filePath) ==='/backup/'){
                                   console.log(`API: Удаление файла из бэкапа ${filePath} (без создания бэкапа бэкапа).`);
                              } else {
                                   console.log(`API: Файл ${filePath} не найден для бэкапа перед удалением.`);
                              }
                             performDeleteTransaction(false);
                         }
                    });
                }

                return { openDB, listFiles, getFile, saveFile, deleteFile };
            })();

            // ----------------------------------------------------------------------
            // --- Main Application Logic ---
            // ----------------------------------------------------------------------

            /** Показывает уведомление */
            function showNotification(message, type = 'success', duration = 3000) {
                const notification = document.createElement('div');
                notification.className = `notification ${type}`;
                notification.textContent = message;
                // Очищаем предыдущие или добавляем? Пока очищаем.
                notificationsElement.innerHTML = '';
                notificationsElement.appendChild(notification);
                if (duration > 0) setTimeout(() => notification.remove(), duration);
            }

            /** Отображает список файлов */
            async function renderFileList(directory) {
                currentDirectory = directory;
                currentPathElement.textContent = directory;
                fileListElement.innerHTML = '<li class="loading">Загрузка...</li>';
                clearEditor(); // Сбрасываем редактор при смене директории
                try {
                    const items = await FileSystemAPI.listFiles(directory);
                    fileListElement.innerHTML = '';
                    if (items.length === 0) {
                        fileListElement.innerHTML = '<li class="loading">Папка пуста</li>';
                    } else {
                        items.forEach(item => {
                            const li = document.createElement('li');
                            li.textContent = item.name;
                            li.dataset.path = item.path;
                            li.className = item.type;
                            li.title = item.path;
                            if (item.type === 'directory') {
                                li.addEventListener('click', () => renderFileList(item.path));
                            } else {
                                li.addEventListener('click', () => loadFileIntoEditor(item.path));
                            }
                            fileListElement.appendChild(li);
                        });
                    }
                } catch (error) {
                    console.error(`Ошибка загрузки списка ${directory}:`, error);
                    showNotification(`Ошибка загрузки файлов: ${error}`, 'error');
                    fileListElement.innerHTML = '<li class="loading error">Ошибка загрузки</li>';
                }
            }

            /** Загружает файл в редактор */
            async function loadFileIntoEditor(filePath) {
                if (isEditorDirty && !confirm("Есть несохраненные изменения. Продолжить?")) return;
                clearEditor();
                fileContentEditor.value = 'Загрузка...';
                fileInfoElement.textContent = `Загрузка: ${Utils.getFileName(filePath)}`;
                selectedFilePath = filePath;
                fileContentEditor.disabled = true; // Блокируем во время загрузки

                try {
                    const fileData = await FileSystemAPI.getFile(filePath);
                    if (fileData) {
                        fileContentEditor.value = fileData.content; // Содержимое уже строка
                        fileInfoElement.textContent = `Файл: ${fileData.name} (${Utils.formatTimestamp(fileData.timestamp)})`;
                        saveButton.disabled = false;
                        deleteButton.disabled = false;
                        isEditorDirty = false;
                        fileContentEditor.disabled = false; // Разблокируем после загрузки

                        // Логика для агентов
                        if (fileData.directory === '/agents/' && fileData.name.endsWith('.json')) {
                            applyAgentConfig(fileData.content, fileData.name);
                        } else {
                             agentConfigDisplay.style.display = 'none';
                             // Если выбран не агент, сбрасываем активную конфигурацию
                             // activeAgentConfig = null; // Пока не сбрасываем, чтобы можно было чатиться, выбрав файл чата
                        }
                         // Логика для чатов
                         if (fileData.directory === '/chats/' && fileData.name.endsWith('.json')) {
                             loadChat(fileData);
                             chatMessageInput.disabled = false; // Разблокируем ввод чата
                             sendMessageButton.disabled = false;
                           // fileContentEditor.disabled = true; // Можно блокировать редактор для чатов
                         } else {
                             currentChatFile = null;
                              chatMessageInput.disabled = true; // Блокируем чат, если выбран не файл чата
                              sendMessageButton.disabled = true;
                         }

                    } else {
                        showNotification(`Файл ${filePath} не найден`, 'error');
                        clearEditor();
                    }
                } catch (error) {
                    console.error(`Ошибка загрузки ${filePath}:`, error);
                    showNotification(`Ошибка загрузки файла: ${error}`, 'error');
                    clearEditor();
                }
            }

            /** Сохраняет файл из редактора */
            async function saveCurrentFile() {
                if (!selectedFilePath || !isEditorDirty) {
                     showNotification(selectedFilePath ? 'Нет изменений для сохранения' : 'Нет файла для сохранения', 'warning', 1500);
                    return;
                }
                console.log(`Сохранение: ${selectedFilePath}`);
                fileStatusElement.textContent = 'Сохранение...';
                saveButton.disabled = true; deleteButton.disabled = true;
                try {
                    const content = fileContentEditor.value;
                    const savedFileData = await FileSystemAPI.saveFile(selectedFilePath, content);
                    showNotification(`Файл ${Utils.getFileName(selectedFilePath)} сохранен.`, 'success');
                    fileStatusElement.textContent = `Сохранено: ${Utils.formatTimestamp(savedFileData.timestamp)}`;
                    isEditorDirty = false;
                    fileInfoElement.textContent = `Файл: ${savedFileData.name} (${Utils.formatTimestamp(savedFileData.timestamp)})`;
                    if (savedFileData.directory === '/agents/' && savedFileData.name.endsWith('.json')) {
                        applyAgentConfig(savedFileData.content, savedFileData.name);
                    }
                    if (['/core/', '/api/', '/utils/'].includes(savedFileData.directory) && savedFileData.name.endsWith('.js')) {
                       showNotification('Изменения кода сохранены. Перезагрузите приложение для применения.', 'warning', 5000);
                    }
                } catch (error) {
                    console.error(`Ошибка сохранения ${selectedFilePath}:`, error);
                    showNotification(`Ошибка сохранения файла: ${error}`, 'error');
                    fileStatusElement.textContent = 'Ошибка сохранения';
                } finally {
                    if(selectedFilePath){ saveButton.disabled = false; deleteButton.disabled = false; }
                }
            }

            /** Удаляет выбранный файл */
            async function deleteCurrentFile() {
                if (!selectedFilePath) return;
                const fileName = Utils.getFileName(selectedFilePath);
                if (!confirm(`Удалить "${fileName}"? Будет создана резервная копия (если это не файл из /backup/).`)) return;
                console.log(`Удаление: ${selectedFilePath}`);
                fileStatusElement.textContent = 'Удаление...';
                saveButton.disabled = true; deleteButton.disabled = true;
                try {
                    await FileSystemAPI.deleteFile(selectedFilePath);
                    showNotification(`Файл "${fileName}" удален.`, 'success');
                    clearEditor();
                    await renderFileList(currentDirectory); // Обновляем список
                } catch (error) {
                    console.error(`Ошибка удаления ${selectedFilePath}:`, error);
                    showNotification(`Ошибка удаления: ${error}`, 'error');
                    fileStatusElement.textContent = 'Ошибка удаления';
                     if(selectedFilePath){ saveButton.disabled = false; deleteButton.disabled = false; } // Разблокировать при ошибке
                }
            }

             /** Очищает редактор */
            function clearEditor() {
                fileContentEditor.value = '';
                fileInfoElement.textContent = 'Выберите файл для просмотра';
                fileStatusElement.textContent = '';
                saveButton.disabled = true;
                deleteButton.disabled = true;
                fileContentEditor.disabled = true;
                selectedFilePath = null;
                isEditorDirty = false;
                agentConfigDisplay.style.display = 'none';
                // Не сбрасываем currentChatFile и активный агент здесь
                // Блокируем чат, если редактор очищен (нет файла чата)
                 if (!currentChatFile) {
                    chatMessageInput.disabled = true;
                    sendMessageButton.disabled = true;
                 }
            }

             /** Отмечает изменения в редакторе */
            function markEditorDirty() {
                if (!isEditorDirty && selectedFilePath) {
                    isEditorDirty = true;
                    fileStatusElement.textContent = 'Несохраненные изменения';
                }
            }

            /** Применяет конфигурацию агента */
             function applyAgentConfig(configContent, agentFileName) {
                 console.log(`Применение конфига ${agentFileName}`);
                 try {
                     activeAgentConfig = JSON.parse(configContent); // Сохраняем конфиг
                     console.log("Активная конфигурация агента:", activeAgentConfig);
                     agentConfigData.textContent = JSON.stringify(activeAgentConfig, null, 2);
                     agentConfigDisplay.style.display = 'block';
                     showNotification(`Конфиг ${activeAgentConfig.name || agentFileName} загружен.`, 'success', 2000);
                 } catch (error) {
                     console.error("Ошибка конфига агента:", error);
                     showNotification(`Ошибка в конфиге ${agentFileName}: ${error}`, 'error');
                     agentConfigData.textContent = `Ошибка загрузки: ${error}`;
                     agentConfigDisplay.style.display = 'block';
                     activeAgentConfig = null; // Сбрасываем конфиг при ошибке
                 }
             }

            /** Создание файла */
            async function createFilePrompt() {
                if (currentDirectory === '/backup/') {
                    showNotification('Нельзя создавать файлы в /backup/', 'error'); return;
                }
                const fileName = prompt(`Имя нового файла в ${currentDirectory} (e.g., my_data.json):`);
                if (!fileName || !fileName.trim()) return;
                const newFilePath = currentDirectory + fileName.trim();
                 try {
                     const existing = await FileSystemAPI.getFile(newFilePath);
                     if (existing) { showNotification(`Файл ${fileName} уже существует.`, 'error'); return; }

                     let initialContent = '';
                     if (fileName.endsWith('.json')) initialContent = '{}';
                     if (currentDirectory === '/chats/' && fileName.endsWith('.json')) initialContent = JSON.stringify({ id: Utils.generateId(), messages: [] }, null, 2);
                     if (currentDirectory === '/agents/' && fileName.endsWith('.json')) initialContent = JSON.stringify({ id: Utils.generateId(), name: "New Agent", configurations: { model: "anthropic/claude-3-haiku" } }, null, 2);
                      if (currentDirectory === '/secrets/' && fileName.endsWith('.json')) initialContent = JSON.stringify({ id: Utils.generateId(), data: {} }, null, 2);

                     console.log(`Создание файла: ${newFilePath}`);
                     await FileSystemAPI.saveFile(newFilePath, initialContent);
                     showNotification(`Файл ${fileName} создан.`, 'success');
                     await renderFileList(currentDirectory);
                     await loadFileIntoEditor(newFilePath);
                 } catch (error) {
                     console.error(`Ошибка создания ${newFilePath}:`, error);
                     showNotification(`Ошибка создания файла: ${error}`, 'error');
                 }
            }

             // --- Логика Чата ---

             /** Загружает чат */
            function loadChat(chatFileData) {
                console.log("Загрузка чата:", chatFileData.filePath);
                chatHistoryElement.innerHTML = ''; // Очистка
                try {
                    const chatData = JSON.parse(chatFileData.content);
                    if (!chatData || !Array.isArray(chatData.messages)) throw new Error("Некорректный формат чата.");
                    currentChatFile = { filePath: chatFileData.filePath, messages: chatData.messages || [] }; // !! Важно: копируем сообщения
                    currentChatFile.messages.forEach(renderChatMessage);
                    chatHistoryElement.scrollTop = chatHistoryElement.scrollHeight;
                } catch (error) {
                    console.error("Ошибка загрузки чата:", error);
                    showNotification(`Ошибка загрузки чата: ${error}`, 'error');
                    renderChatMessage({ sender:'error', text:`Ошибка загрузки истории чата ${Utils.getFileName(chatFileData.filePath)}.`});
                    currentChatFile = null;
                }
            }

             /** Рендерит сообщение чата */
            function renderChatMessage(message) {
                const msgDiv = document.createElement('div');
                msgDiv.classList.add('chat-message', message.sender); // sender может быть 'user', 'agent', 'error'
                const senderSpan = document.createElement('div');
                senderSpan.classList.add('sender');
                senderSpan.textContent = message.sender === 'user' ? 'Вы' : (message.sender === 'agent' ? (activeAgentConfig?.name || 'Агент') : 'Система');
                const textNode = document.createElement('div');
                textNode.textContent = message.text; // TODO: Sanitize HTML?
                msgDiv.appendChild(senderSpan);
                msgDiv.appendChild(textNode);
                if(message.timestamp) {
                    const timeSpan = document.createElement('div');
                    timeSpan.classList.add('timestamp');
                    timeSpan.textContent = Utils.formatTimestamp(message.timestamp);
                    msgDiv.appendChild(timeSpan);
                }
                chatHistoryElement.appendChild(msgDiv);
            }

            /** Отправляет сообщение и получает ответ от API */
            async function sendMessage() {
                const messageText = chatMessageInput.value.trim();
                const apiKey = apiKeyInput.value.trim();

                if (!messageText) return;
                if (!currentChatFile) { showNotification("Выберите файл чата.", 'error'); return; }
                if (!apiKey) { showNotification("Введите VseGPT API ключ.", 'error'); apiKeyInput.focus(); return; }
                 if (!activeAgentConfig || !activeAgentConfig.configurations || !activeAgentConfig.configurations.model) {
                     showNotification("Загрузите конфигурацию агента с указанной моделью (e.g., /agents/example-agent.json)", 'error');
                     return;
                 }

                 chatMessageInput.disabled = true; sendMessageButton.disabled = true; // Блокируем на время запроса

                // 1. Отображаем сообщение пользователя
                const userMessage = { sender: 'user', text: messageText, timestamp: Date.now() };
                currentChatFile.messages.push(userMessage);
                renderChatMessage(userMessage);
                chatMessageInput.value = '';
                chatHistoryElement.scrollTop = chatHistoryElement.scrollHeight;

                // 2. Готовим запрос к API
                 const apiEndpoint = "https://api.vsegpt.ru/v1/chat/completions";
                 const model = activeAgentConfig.configurations.model;
                 const temperature = activeAgentConfig.configurations.temperature || 0.7;
                 const max_tokens_config = activeAgentConfig.configurations.max_tokens || 1000; // Уменьшил дефолт

                 // Формируем историю для API (можно добавить system prompt из конфига, если есть)
                 const apiMessages = [];
                 // if (activeAgentConfig.configurations.system_prompt) {
                 //     apiMessages.push({ role: "system", content: activeAgentConfig.configurations.system_prompt });
                 // }
                 // Добавляем последние N сообщений из истории (простое решение)
                 const historyToSend = currentChatFile.messages.slice(-10); // Берем последние 10 сообщений (user/agent)
                 historyToSend.forEach(msg => {
                      if(msg.sender === 'user' || msg.sender === 'agent'){ // Игнорируем сообщения об ошибках
                           apiMessages.push({ role: msg.sender === 'user' ? 'user' : 'assistant', content: msg.text });
                      }
                 });


                 const requestBody = {
                     model: model,
                     messages: apiMessages, // Передаем историю
                     temperature: temperature,
                     n: 1,
                     max_tokens: max_tokens_config,
                 };

                 console.log("API Request Body:", JSON.stringify(requestBody, null, 2));
                 showNotification("Отправка запроса к AI...", "success", 1500);

                 try {
                     const response = await fetch(apiEndpoint, {
                         method: 'POST',
                         headers: {
                             'Content-Type': 'application/json',
                            'Authorization': `Bearer ${apiKey}`,
                            'X-Title': "SmartAssistantSF" // Optional header
                         },
                         body: JSON.stringify(requestBody)
                     });

                     if (!response.ok) {
                          // Пытаемся прочитать тело ошибки от API
                         let errorData = null;
                         try { errorData = await response.json(); } catch (e) {}
                         console.error("API Error Response:", errorData);
                         throw new Error(`Ошибка API: ${response.status} ${response.statusText}. ${errorData?.error?.message || ''}`);
                     }

                     const responseData = await response.json();
                     console.log("API Response:", responseData);

                     const agentResponseText = responseData.choices?.[0]?.message?.content?.trim();

                     if (!agentResponseText) {
                         throw new Error("API не вернул текст ответа.");
                     }

                      // 3. Отображаем ответ агента
                     const agentMessage = { sender: 'agent', text: agentResponseText, timestamp: Date.now() };
                     currentChatFile.messages.push(agentMessage);
                     renderChatMessage(agentMessage);

                 } catch (error) {
                     console.error("Ошибка вызова API:", error);
                     const errorMessage = { sender: 'error', text: `Ошибка: ${error.message}`, timestamp: Date.now() };
                     currentChatFile.messages.push(errorMessage); // Добавляем ошибку в историю
                     renderChatMessage(errorMessage);
                     showNotification(`Ошибка API: ${error.message}`, 'error', 5000);
                 } finally {
                     chatHistoryElement.scrollTop = chatHistoryElement.scrollHeight;
                     chatMessageInput.disabled = false; sendMessageButton.disabled = false; // Разблокируем ввод
                     // 4. Сохраняем обновленный чат (даже если была ошибка, т.к. ошибка добавлена в историю)
                     await saveCurrentChat();
                }
            }

            /** Сохраняет текущий чат */
            async function saveCurrentChat() {
                if (!currentChatFile) return;
                console.log(`Сохранение чата: ${currentChatFile.filePath}`);
                try {
                    const chatDataToSave = {
                        id: Utils.getFileName(currentChatFile.filePath).replace('.json', ''),
                        messages: currentChatFile.messages // Сохраняем всю историю
                    };
                    // Вызываем saveFile без await, чтобы не блокировать UI, но логируем результат
                    FileSystemAPI.saveFile(currentChatFile.filePath, chatDataToSave)
                        .then(savedData => {
                            console.log(`Чат ${currentChatFile.filePath} сохранен.`);
                            // Обновляем редактор, если чат открыт там
                            if (selectedFilePath === currentChatFile.filePath) {
                                isEditorDirty = false; // Считаем сохраненным
                                fileContentEditor.value = savedData.content;
                                fileInfoElement.textContent = `Файл: ${savedData.name} (${Utils.formatTimestamp(savedData.timestamp)})`;
                                fileStatusElement.textContent = `Сохранено: ${Utils.formatTimestamp(savedData.timestamp)}`;
                            }
                        })
                        .catch(err => {
                             console.error(`Фоновая ошибка сохранения чата ${currentChatFile.filePath}:`, err);
                             // Можно показать тихое уведомление
                             // showNotification(`Фоновая ошибка сохранения чата: ${err}`, 'error', 2000);
                        });
                } catch (error) { // Ошибка обычно при JSON.stringify, маловероятно
                    console.error(`Критическая ошибка подготовки сохранения чата ${currentChatFile.filePath}:`, error);
                    showNotification(`Ошибка подготовки сохранения чата: ${error}`, 'error');
                }
            }


            // --- Привязка событий ---
            saveButton.addEventListener('click', saveCurrentFile);
            deleteButton.addEventListener('click', deleteCurrentFile);
            fileContentEditor.addEventListener('input', markEditorDirty);
            rootButton.addEventListener('click', () => renderFileList('/'));
            createFileButton.addEventListener('click', createFilePrompt);
            chatMessageInput.addEventListener('keypress', (e) => { if (e.key === 'Enter') sendMessage(); });
            sendMessageButton.addEventListener('click', sendMessage);

            // --- Запуск приложения ---
            try {
                await FileSystemAPI.openDB();
                showNotification('База данных готова.', 'success', 1500);
                await renderFileList('/');
                clearEditor();
             } catch (error) {
                console.error("Критическая ошибка инициализации:", error);
                showNotification(`Критическая ошибка: ${error}.`, 'error', 0);
                 fileListElement.innerHTML = '<li class="loading error">Ошибка инициализации БД</li>';
            }
             console.log('Smart Assistant (SF): Инициализация завершена.');

        })(); // Конец IIFE
    </script>
</body>
</html>
